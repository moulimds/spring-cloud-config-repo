spring:
  datasource:
    url: jdbc:mysql://localhost:3306/notification_db
    username: root
    password: "@Bluemoon257"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/